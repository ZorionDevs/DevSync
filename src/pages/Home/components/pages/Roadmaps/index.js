import React, { useState, useEffect } from 'react';
import { View, Text } from 'react-native';

export default function Roadmaps(){
  return(
    <View>
      <Text style={{color: '#fff'}}>Roadmaps</Text>
    </View>
  )
}